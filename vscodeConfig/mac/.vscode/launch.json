{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenGL (macOS)",
            "type": "cppdbg",              
            "request": "launch",
            "program": "${workspaceFolder}/build/OpenGL", // ✅ auto-picks correct executable
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/OpenGL",   // ✅ root directory (so shaders/resources load correctly)
            "environment": [],
            "console": "integratedTerminal",
            "MIMode": "lldb",              // ✅ lldb is default macOS debugger
            "preLaunchTask": "build OpenGL"
        }
    ]
}
